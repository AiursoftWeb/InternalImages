stages:
  - deploy

.deploy_template:
  stage: deploy
  variables:
    PLATFORMS: "linux/amd64,linux/arm64"
  before_script:
    - if [[ "$CI_PROJECT_NAMESPACE" != "aiursoft" ]]; then
        echo "CI pipeline will only run for the Aiursoft namespace.";
        exit 1;
      fi
    - echo "Logging in to Docker Registry..."
    - echo "$LOCAL_DOCKER_PASSWORD" | docker login hub.aiursoft.com -u "$LOCAL_DOCKER_USERNAME" --password-stdin
    - docker buildx use aiur-global-builder
  script:
    - IMAGE_TARGET="hub.aiursoft.com/$CI_PROJECT_NAMESPACE/$CI_PROJECT_NAME/$IMAGE_NAME:latest"
    - echo "Building image $IMAGE_TARGET"
    - docker buildx build "$CONTEXT" --platform "$PLATFORMS" -t "$IMAGE_TARGET" --push
  rules:
    - if: '$CI_COMMIT_BRANCH == "master"'

deploy_anduinos_internal:
  extends: .deploy_template
  variables:
    IMAGE_NAME: "anduinos-internal"
    CONTEXT: "./AnduinOS-Internal"

deploy_anduinos_internal_with_docker:
  extends: .deploy_template
  needs:
    - job: deploy_anduinos_internal
      optional: true
  variables:
    IMAGE_NAME: "anduinos-with-docker"
    CONTEXT: "./AnduinOS-with-docker"

deploy_exp_ppa_mirror:
  extends: .deploy_template
  variables:
    IMAGE_NAME: "exp-ppa-mirror"
    CONTEXT: "./ExpPpaMirror"

deploy_lab:
  extends: .deploy_template
  needs:
    - job: deploy_anduinos_internal
      optional: true
  variables:
    IMAGE_NAME: "lab"
    CONTEXT: "./Lab"

deploy_kiwix:
  extends: .deploy_template
  needs:
    - job: deploy_anduinos_internal
      optional: true
  variables:
    IMAGE_NAME: "kiwix"
    CONTEXT: "./Kiwix"

deploy_dotnet:
  extends: .deploy_template
  needs:
    - job: deploy_anduinos_internal
      optional: true
  variables:
    IMAGE_NAME: "dotnet"
    CONTEXT: "./Dotnet"

deploy_dotnet_only_runtime:
  extends: .deploy_template
  needs:
    - job: deploy_anduinos_internal
      optional: true
  variables:
    IMAGE_NAME: "dotnetonlyruntime"
    CONTEXT: "./DotnetOnlyRuntime"

deploy_gitmirror:
  extends: .deploy_template
  needs:
    - job: deploy_anduinos_internal
      optional: true
  variables:
    IMAGE_NAME: "gitmirror"
    CONTEXT: "./GitMirror"

deploy_minecraft:
  extends: .deploy_template
  needs:
    - job: deploy_anduinos_internal
      optional: true
  variables:
    IMAGE_NAME: "minecraft"
    CONTEXT: "./Minecraft"

deploy_nvidia:
  extends: .deploy_template
  variables:
    IMAGE_NAME: "nvidia"
    CONTEXT: "./Nvidia"
    PLATFORMS: "linux/amd64"

deploy_swinir:
  extends: .deploy_template
  needs:
    - job: deploy_nvidia
      optional: true
  variables:
    IMAGE_NAME: "swinir"
    CONTEXT: "./SwinIR"
    PLATFORMS: "linux/amd64"

deploy_youtubedl:
  extends: .deploy_template
  needs:
    - job: deploy_anduinos_internal
      optional: true
  variables:
    IMAGE_NAME: "youtubedl"
    CONTEXT: "./Youtubedl"

deploy_reportertts:
  extends: .deploy_template
  needs:
    - job: deploy_nvidia
      optional: true
  variables:
    IMAGE_NAME: "reportertts"
    CONTEXT: "./ReporterTTS"
    PLATFORMS: "linux/amd64"

deploy_paddleocr:
  extends: .deploy_template
  needs:
    - job: deploy_anduinos_internal
      optional: true
  variables:
    IMAGE_NAME: "paddleocr"
    CONTEXT: "./PaddleOCR"
    PLATFORMS: "linux/amd64"
deploy_codexproxy:
  extends: .deploy_template
  needs:
    - job: deploy_anduinos_internal
      optional: true
  variables:
    IMAGE_NAME: "codexproxy"
    CONTEXT: "./CodexProxy"
    PLATFORMS: "linux/amd64"